import { makeStyles } from '@material-ui/core/styles';
export const useAuthorItemStyles = makeStyles({
  avatar: {
    margin: 10
  },
  authorItem: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column'
  }
});
